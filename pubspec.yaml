name: riverflow
description: The best a dev can get
version: 0.0.5
homepage:

environment:
  sdk: ">=2.16.2 <3.0.0"
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter
  
  hooks_riverpod: ^1.0.4
  logger: ^1.1.0
  duration: ^3.0.11

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^1.0.4


# The following section is specific to Flutter.
flutter:

  
